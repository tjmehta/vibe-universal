# Environment Variables
# Copy to .env.local and fill in values
#
# Pull from Vercel: pnpm env:pull:vercel
# Pull from Convex: pnpm env:pull:convex
#
# NOTE FOR EXPO:
#   Expo looks for .env in apps/native/, not the monorepo root.
#   Either copy EXPO_PUBLIC_* vars to apps/native/.env,
#   or convert app.json to app.config.js to load from root.

# =============================================================================
# CONVEX (required)
# =============================================================================
# Get from: https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# For Expo native app (put in apps/native/.env)
EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# =============================================================================
# APP CONFIG
# =============================================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# AUTH (optional - for OAuth providers)
# =============================================================================
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# =============================================================================
# ANALYTICS (optional)
# =============================================================================
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=
